# Prime-resonance channels — speculative extension

Status: **SPECULATIVE ONLY**.

This folder intentionally contains the whole prime-resonance / long-cycle idea,
including the use of the neighboring prime `139` and the composite base

```text
N0 = 137 * 139 = 19043
```

Nothing in this folder should be presented as a canonical derivation of alpha,
QED, QCD, cosmology, history, climate cycles, or civilizational dynamics.

Boundary:

- `137` is the alpha-adjacent anchor already studied in the alpha research track.
- `139` is **not** a canonical UBT constant here; it is only a speculative neighboring-prime harmonic partner.
- All period labels, civilizational / climate / cultural cycle interpretations,
  and eta-like resonance scores are exploratory heuristics.
- Data outputs stay in this folder under `data/`, not under `experiments/` and not under the canonical alpha track.

Regenerate tables from the repository root with:

```bash
python speculative_extensions/prime_resonance_channels/generate_prime_resonance_table.py
```

Expected outputs:

```text
speculative_extensions/prime_resonance_channels/data/prime_resonance_harmonic_table_full.csv
speculative_extensions/prime_resonance_channels/data/prime_resonance_top250_by_score.csv
speculative_extensions/prime_resonance_channels/data/prime_resonance_landmark_channels.csv
speculative_extensions/prime_resonance_channels/data/prime_resonance_table_summary.md
speculative_extensions/prime_resonance_channels/data/last_run_output.txt
```
