# Patch notes — move p=139 mirror-sector material to speculative extension

This patch separates interpretive `p = 139` / Set B / mirror-sector material from the canonical tree.

Moved/preserved under:

```text
speculative_extensions/alpha_139_mirror_sector/
```

Canonical files may still use `139` as:

- a neighbouring prime in stability scans,
- a numerical control/benchmark,
- a falsification or kill-condition test.

Canonical files should not claim that `p = 139` is an established physical mirror sector.
