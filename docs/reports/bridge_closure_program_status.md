# Bridge Closure Program status

This patch creates a dedicated bridge-closure track:

```text
research_tracks/BRIDGE_CLOSURE/
```

Main correction:

```text
R_psi=1 is already derived as the dimensionless self-dual radius.
The open bridge is physical scale calibration, not R_psi fixation.
```

Current high-priority bridges:

1. B2 Alpha Layer2 / G137-B bridge — strong MC+/L1-conditional; not bridge-proven until `B` and the `n ln n` potential are derived from `S[Theta]`.
2. B3 Weinberg odd-spinor threshold bridge — bridge-proven.
3. B4 Self-dual radius bridge — dimensionless R_psi=1 closed; physical scale calibration remains partial.
4. B5 Physical scale calibration — next target.
5. B6 Hypercharge bridge — partial; C2-ii/C2-iii still open.
6. B7 Metric/GR bridge — L1 on-shell, off-shell open.
7. B8 QCD colour bridge — partial; needs theorem-level colour-sector closure.

Recommended immediate next targets:

```text
B2 / G137-B alpha coefficient closure.
B5 physical scale calibration at R_psi=1.
```

Specifically: derive the physical unit Lambda_psi or condensate normalisation
that maps the self-dual psi unit to M_UBT ≈ 2e16 GeV.


## Update: G137-B final bridge-level closure

G137-B is now closed at UBT bridge level by `research_tracks/T3_ALPHA/g137b_final_bridge_closure.tex`. The alpha route should be described as bridge-level derived inside the current UBT bridge package, not externally proven. Remaining work is independent audit of the bridge assumptions.
