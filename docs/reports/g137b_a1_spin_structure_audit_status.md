# G137-B A1 complete spin-structure trace audit status

A1 audits the bridge:

```text
Z_compact^1real = eta^-2 theta2 theta3 theta4
```

Result:

```text
A1 = AUDITED-L1-COND
```

Reason:

- `theta3` comes from periodic neutral compact winding.
- `theta2` comes from SU(2) Scherk-Schwarz shifted charged tower.
- `theta4` comes from odd-winding parity trace.
- Product, not sum, follows from independent Gaussian determinant blocks.
- Including all three is the minimal triad-complete trace under `Im H`.

Residual risk:

```text
A reviewer may challenge the triad-to-spin-structure map.
```

Next recommended audit: A2 four-channel geometric mean.
