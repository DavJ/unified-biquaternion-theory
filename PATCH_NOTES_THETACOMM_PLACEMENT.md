# Patch notes — ThetaComm placement

This patch adds an explicit quarantine location for ThetaComm:

```text
speculative_extensions/thetacomm/README.md
```

It also updates references so the claim matrix points to the real path and
removes a misleading historical author label from a legacy mass-hierarchy TeX
file.  The legacy file is not moved to ThetaComm because its content is about
mass hierarchy, not consciousness communication; only the ambiguous author label
was corrected.

No canonical physics claim is changed.
