# Patch notes — Theta0 electroweak vacuum orbit theorem

Adds:

```text
research_tracks/T3_ALPHA/theta0_electroweak_vacuum_orbit_theorem.tex
```

Updates:

```text
research_tracks/T3_ALPHA/alpha_derivation_complete.tex
research_tracks/T3_ALPHA/electroweak_em_projection_theorem.tex
research_tracks/T3_ALPHA/em_modular_coupling_identification.tex
docs/reports/alpha_theta0_vacuum_orbit_closure.md
```

Purpose: close the representation/orbit part of the final alpha proof condition.

Result:

```text
non-zero minimal UBT electroweak doublet vacuum
=> gauge-equivalent to Phi0 = (0, v/sqrt(2))^T with Y = 1
=> Q_EM = T3 + Y/2
=> compact unit-charge U(1)_EM
=> n = alpha^-1
```

Remaining structural task:

```text
prove V(Theta) has a non-zero minimum in the minimal electroweak doublet sector
```

No numerical fit is added.
