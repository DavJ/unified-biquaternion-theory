% © 2026 Ing. David Jaroš — CC BY-NC-ND 4.0
%
% This work is licensed under a Creative Commons Attribution-NonCommercial-NoDerivatives
% 4.0 International License (CC BY-NC-ND 4.0).
%
% License History: Earlier drafts (up to v0.3) were released under CC BY 4.0.
% From v0.4 onward, all material is released under CC BY-NC-ND 4.0 to protect
% the integrity of the theoretical work during ongoing academic development.
%
% See LICENSE.md for full license text.
%
% Track: quantum_ubt — CMB prediction
% File: research_tracks/quantum_ubt/delta_neff_prediction.tex
% Purpose: Precise computation of ΔN_eff CMB-S4 prediction from UBT KK sector.
% Status: OPEN — calculation completed; observational consistency discussion pending.
% Computation: tools/delta_neff_calc.py
% Date: 2026-05-17

\documentclass[12pt,a4paper]{article}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb,amsthm,booktabs}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{hyperref}

\newtcolorbox{statusbox}[1][]{%
  colback=gray!8!white,colframe=black!60,arc=3pt,boxrule=1pt,
  fonttitle=\bfseries,title=Status Summary,#1}
\newtcolorbox{predbox}[1][]{%
  colback=blue!4!white,colframe=blue!55!black,arc=3pt,boxrule=1.2pt,
  fonttitle=\bfseries,#1}
\newtcolorbox{warnbox}[1][]{%
  colback=orange!5!white,colframe=orange!60!black,arc=3pt,boxrule=1pt,
  fonttitle=\bfseries,title=Warning,#1}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}

\title{UBT Prediction: $\Delta N_{\rm eff}$ from KK Sector\\[4pt]
       \large Track: quantum\_ubt / T3 Priority}
\author{Ing.\ David Jaro\v{s}}
\date{2026-05-17}

\begin{document}
\maketitle

\begin{abstract}
The UBT Scherk--Schwarz twist on $S^1_\psi$ generates $N_{\rm eff}^{(KK)}=12$
charged Kaluza--Klein modes with masses $m_n \sim n\,M_{\rm Pl}$.
These modes decouple from the thermal bath at $T_{\rm dec} \sim M_{\rm Pl}$
and produce a relic contribution to the CMB effective number of relativistic
degrees of freedom.  Using the standard cosmological formula we find:
$\Delta N_{\rm eff}^{(\rm per\ mode)} \approx 0.047$, in agreement with the
value 0.046 quoted in \texttt{STATUS\_OF\_UBT.md}.
The total contribution from all 12 modes ($\approx 0.562$) is in tension with
current Planck+ACT data and is flagged as an open consistency issue.
\textbf{Alpha: NOT DERIVED.}
\end{abstract}

\tableofcontents

%──────────────────────────────────────────────────────────────────────────────
\section{Setup: UBT KK Modes and Decoupling}
\label{sec:setup}
%──────────────────────────────────────────────────────────────────────────────

From \texttt{research\_tracks/quantum\_ubt/su2\_twist\_neff12.tex}~[L1],
the Scherk--Schwarz boundary condition
\[
  \Theta(\psi + 2\pi R_\psi) = \sigma_3\,\Theta\,\sigma_3
\]
on $S^1_\psi$ with $R_\psi \sim \ell_{\rm Pl}$ generates $N_{\rm eff}^{(KK)}=12$
effective charged bosonic KK modes in the one-loop effective potential.

The $n$-th KK excitation has mass
\[
  m_n = \frac{n}{R_\psi} \sim n\,M_{\rm Pl},
  \qquad n = 1,2,3,\ldots
\]
Since $m_1 \sim M_{\rm Pl} \gg T_{\rm BBN} \sim 1\,\mathrm{MeV}$,
all KK modes are non-relativistic at BBN and have decoupled
from the thermal bath at $T_{\rm dec} \sim M_{\rm Pl}$.

%──────────────────────────────────────────────────────────────────────────────
\section{Standard Relic Formula}
\label{sec:formula}
%──────────────────────────────────────────────────────────────────────────────

For a species with $g_X$ real bosonic degrees of freedom that decoupled
from the thermal bath in radiation domination at temperature $T_{\rm dec}$,
the contribution to $N_{\rm eff}$ today is
\begin{equation}
  \label{eq:delta_neff_standard}
  \Delta N_{\rm eff} = g_X\,\left(\frac{43/4}{g_*(T_{\rm dec})}\right)^{\!4/3},
\end{equation}
where $g_*(T_{\rm dec})$ is the effective relativistic d.o.f.\ at decoupling,
and the factor $43/4 = 2 + (7/8)\cdot 6$ accounts for photons and three SM
neutrino species.

\subsection*{Derivation of equation~(\ref{eq:delta_neff_standard})}

After decoupling the species temperature $T_X$ evolves as
$T_X \propto 1/a$ (no further heating).  Entropy conservation
in the SM thermal bath gives
\[
  \frac{T_X}{T_\gamma} = \left(\frac{g_{*s}(T_0)}{g_{*s}(T_{\rm dec})}\right)^{1/3}
\]
where $g_{*s}(T_0) = 43/11$ counts photons and SM neutrinos.
Today $T_\nu = (4/11)^{1/3} T_\gamma$, so
\[
  \frac{T_X}{T_\nu} = \frac{T_X/T_\gamma}{T_\nu/T_\gamma}
  = \left(\frac{43/4}{g_*(T_{\rm dec})}\right)^{1/3},
\]
and $\Delta N_{\rm eff} = g_X(T_X/T_\nu)^4$ yields~(\ref{eq:delta_neff_standard}).

%──────────────────────────────────────────────────────────────────────────────
\section{Numerical Result}
\label{sec:result}
%──────────────────────────────────────────────────────────────────────────────

\subsection{Input parameters}

\begin{center}
\begin{tabular}{ll}
\toprule
Parameter & Value \\
\midrule
$N_{\rm eff}^{(KK)}$ (UBT SU(2) twist) & 12 [L1] \\
$g_*(T_{\rm dec}) \approx g_*(T_{\rm Pl})$ (SM) & 106.75 \\
CMB-S4 sensitivity & $\sigma(\Delta N_{\rm eff}) \approx 0.03$ \\
Planck+ACT bound (2024) & $\Delta N_{\rm eff} < 0.28$ (95\% CL) \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Calculation}

Per KK mode ($g_X = 1$):
\[
  \Delta N_{\rm eff}^{(\rm mode)} =
  \left(\frac{43/4}{106.75}\right)^{4/3}
  = (0.1007)^{4/3}
  \approx \mathbf{0.0469}.
\]

Total from $N_{\rm eff}^{(KK)} = 12$ modes:
\[
  \Delta N_{\rm eff}^{(\rm total)} = 12 \times 0.0469 \approx \mathbf{0.562}.
\]

Numerical verification: \texttt{tools/delta\_neff\_calc.py} confirms both values.

\subsection{Comparison with quoted value}

The value $\Delta N_{\rm eff} \approx 0.046$ quoted in \texttt{STATUS\_OF\_UBT.md}
corresponds to the \emph{per-mode} contribution.  The total from all 12 modes
is $\approx 0.562$.

%──────────────────────────────────────────────────────────────────────────────
\section{Prediction Card}
\label{sec:prediction}
%──────────────────────────────────────────────────────────────────────────────

\begin{predbox}[title={UBT Prediction: $\Delta N_{\rm eff}$ (per KK mode)}]
\[
  \Delta N_{\rm eff}^{(\rm mode)}(\mathrm{UBT}) \approx 0.047
\]
CMB-S4 sensitivity: $\sigma(\Delta N_{\rm eff}) \approx 0.03$\\
Status: \textbf{testable per mode — above CMB-S4 threshold}\\[4pt]
Fail criterion: $\Delta N_{\rm eff} < 0$ or $\Delta N_{\rm eff} > 1$ would
falsify the UBT KK sector.
\end{predbox}

%──────────────────────────────────────────────────────────────────────────────
\section{Observational Consistency Note}
\label{sec:consistency}
%──────────────────────────────────────────────────────────────────────────────

\begin{warnbox}
The \emph{total} contribution $\Delta N_{\rm eff}^{(\rm total)} \approx 0.562$
from all 12 KK modes exceeds the current Planck+ACT bound
$\Delta N_{\rm eff} < 0.28$ (95\% CL) by a factor of $\sim 2$.
This is a potential tension that requires resolution before the prediction
is publishable as a clean result.
\end{warnbox}

Several mitigating factors require investigation:
\begin{enumerate}
\item \textbf{Mode stability}: Not all 12 KK modes may be stable on cosmological
  time-scales.  Short-lived KK modes decay and do not contribute as
  relic radiation.  A decay-rate analysis is needed.
\item \textbf{g* counting}: If $g_*(T_{\rm dec})$ is significantly larger than
  106.75 (e.g.\ including tower modes not yet integrated out),
  the per-mode contribution decreases.  For $g_* = 427$:
  $\Delta N_{\rm eff}^{(\rm mode)} \approx 0.0074$, total $\approx 0.089$
  (still above CMB-S4 but within Planck+ACT).
\item \textbf{Non-equilibrium}: The KK modes may never have been in thermal
  equilibrium if their coupling to the SM thermal bath is suppressed
  by powers of $M_{\rm Pl}^{-1}$.
\end{enumerate}
Resolution of these issues is tracked as~\textbf{[OPEN]} in this file.

%──────────────────────────────────────────────────────────────────────────────
\begin{statusbox}
$\Delta N_{\rm eff}$ per KK mode: $\approx 0.047$ [computation, standard formula]\\
$\Delta N_{\rm eff}$ total (12 modes): $\approx 0.562$ [OPEN — tension with Planck+ACT]\\
CMB-S4 sensitivity ($\sigma \approx 0.03$): per-mode signal is above threshold\\
Mode stability analysis: [OPEN]\\
g*(T\_Pl) > 106.75 scenario: [OPEN — may resolve tension]\\
\textbf{Alpha: NOT DERIVED.}
\end{statusbox}
%──────────────────────────────────────────────────────────────────────────────

\end{document}
