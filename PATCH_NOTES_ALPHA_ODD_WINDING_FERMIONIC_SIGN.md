# Patch notes — Path B odd-winding fermionic EW sign route

Adds:

- `research_tracks/T3_ALPHA/odd_winding_fermionic_ew_sign_theorem.tex`
- `docs/reports/alpha_pathB_odd_winding_fermionic_sign.md`

Updates:

- `research_tracks/T3_ALPHA/alpha_derivation_complete.tex`

This patch uses the existing canonical result that odd winding modes carry a
Grassmann/Berezin measure.  Therefore an EW doublet read out from the odd-winding
spinor sector has a fermionic determinant and contributes the negative quadratic
term required for electroweak symmetry breaking.

Remaining target theorem:

```math
Pi_EW Theta subset H_{odd,spinor}.
```

If that Layer2 readout theorem is established, Path B reaches PROVEN status.  If
not, the fallback is the explicit EW-breaking postulate.
