# Tip: ensure Makefile has explicit targets (optional)
# ALL_MAIN=ubt_2_main.tex
# core:
# 	$(PDFLATEX) -interaction=nonstopmode $(CORE_MAIN)
# all:
# 	$(PDFLATEX) -interaction=nonstopmode $(ALL_MAIN)
