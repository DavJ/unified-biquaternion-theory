name: Build all LaTeX PDFs

permissions:
  contents: write

on:
  push:
    branches: [master]
    paths-ignore:
      - 'reports/latex_build/**'
      - 'docs/pdfs/**'
  pull_request:
  workflow_dispatch:
    inputs:
      include_archive:
        description: 'Also compile archived and legacy standalone TeX roots'
        required: false
        default: false
        type: boolean
      jobs:
        description: 'Parallel LaTeX workers'
        required: false
        default: '4'
        type: string
      timeout:
        description: 'Timeout in seconds per document'
        required: false
        default: '180'
        type: string

concurrency:
  group: latex-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compile-all:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install LaTeX toolchain
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            latexmk biber python3-pygments poppler-utils \
            texlive-latex-base texlive-latex-recommended texlive-latex-extra \
            texlive-fonts-recommended texlive-science \
            texlive-xetex texlive-luatex texlive-bibtex-extra
          python3 -m pip install --upgrade pip
          python3 -m pip install pytest pyyaml

      - name: Normalize generated audit provenance state
        run: |
          python3 tools/mark_latex_audit_report.py
          python3 tools/apply_provenance_headers.py --report \
            > PROVENANCE_INVENTORY_2026-08-01.txt
          python3 tools/regenerate_sha256sums.py

      - name: Enforce signed provenance release gate
        run: |
          python3 tools/apply_provenance_headers.py --check
          python3 tools/verify_provenance_review.py
          python3 -m pytest -q \
            tests/test_provenance_headers.py::test_tier_map_is_signed_off \
            tests/test_provenance_headers.py::test_tier_map_is_editorially_bounded \
            tests/test_provenance_headers.py::test_all_markable_sources_match_the_tier_map \
            tests/test_provenance_headers.py::test_distributed_tex_roots_declare_matching_tiers \
            tests/test_provenance_headers.py::test_provenance_inventory_matches_current_tree \
            tests/test_provenance_review.py

      - name: Compile every standalone TeX root
        shell: bash
        env:
          DISPATCH_INCLUDE_ARCHIVE: ${{ inputs.include_archive || false }}
          DISPATCH_JOBS: ${{ inputs.jobs || '4' }}
          DISPATCH_TIMEOUT: ${{ inputs.timeout || '180' }}
        run: |
          set -uo pipefail
          args=(
            --repo .
            --report-dir reports/latex_build
            --build-dir build/latex-audit
            --jobs "$DISPATCH_JOBS"
            --timeout "$DISPATCH_TIMEOUT"
          )
          if [[ "$DISPATCH_INCLUDE_ARCHIVE" == "true" ]]; then
            args+=(--include-archive)
          fi
          # Document-level failures are recorded by the tool and never stop the
          # remaining compilation queue. Infrastructure errors still fail here.
          python3 tools/latex_audit.py "${args[@]}"

      - name: Normalize regenerated audit provenance state
        if: success()
        run: |
          python3 tools/mark_latex_audit_report.py
          python3 tools/apply_provenance_headers.py --report \
            > PROVENANCE_INVENTORY_2026-08-01.txt

      - name: Publish curated canonical PDFs
        if: success()
        run: |
          python3 tools/publish_latex_pdfs.py \
            --repo . \
            --build-dir build/latex-audit/pdfs \
            --map .github/latex_publish_map.tsv

      - name: Verify curated PDF provenance and refresh checksums
        if: success()
        run: |
          python3 tools/verify_pdf_provenance.py --require-all
          python3 tools/regenerate_sha256sums.py
          python3 tools/verify_sha256sums.py --quiet
          python3 tools/apply_provenance_headers.py --check
          python3 -m pytest -q \
            tests/test_provenance_headers.py::test_provenance_inventory_matches_current_tree \
            tests/test_sha256sums_integrity.py

      - name: Upload all successful PDFs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: latex-pdfs-${{ github.run_id }}
          path: build/latex-audit/pdfs/**/*.pdf
          if-no-files-found: warn
          retention-days: 30

      - name: Upload complete LaTeX audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: latex-audit-${{ github.run_id }}
          path: reports/latex_build/
          if-no-files-found: error
          retention-days: 90

      - name: Commit report and curated PDFs
        if: success() && github.event_name == 'push' && github.ref == 'refs/heads/master'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Never attach PDFs built from an older source tree to a newer human
          # commit. Artifacts and logs have already been uploaded above; when
          # master advanced during this long build, the newer push will trigger
          # its own fresh audit.
          git fetch origin master
          remote_head="$(git rev-parse origin/master)"
          if [[ "$remote_head" != "$GITHUB_SHA" ]]; then
            echo "Master advanced from $GITHUB_SHA to $remote_head; refusing to commit stale PDFs/report."
            exit 0
          fi

          git add -f reports/latex_build
          git add docs/pdfs PROVENANCE_INVENTORY_2026-08-01.txt SHA256SUMS.txt
          if git diff --cached --quiet; then
            echo "No report, provenance inventory, or PDF changes to commit"
            exit 0
          fi
          git commit -m "chore: refresh LaTeX audit and PDFs [skip ci]"
          git push origin HEAD:master
