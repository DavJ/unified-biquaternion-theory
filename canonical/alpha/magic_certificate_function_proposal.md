# Magic certificate function proposal — moved to speculative extension

Status: moved / non-canonical proposal.

The version containing the twin-prime condition `F(139)=0` has been moved to:

```text
speculative_extensions/alpha_139_mirror_sector/magic_certificate_function_proposal.md
```

Reason: the `F(139)=0` twin-prime condition is a speculative constraint, not a canonical alpha derivation. Canonical alpha files may still use 139 as a control prime in scans or kill conditions.
