% tex/snippets_insert.tex
% Include into emergent_alpha_from_ubt.tex where appropriate.

\subsection*{Computed $\alpha$ samples (from CSV)}
We present a few computed points of $\alpha(\mu)$ exported by our two-loop strict pipeline.

\pgfplotstableset{columns/mu/.style={column name={$\mu$}},
                  columns/alpha/.style={column name={$\alpha(\mu)$}, fixed, fixed zerofill, precision=12},
                  columns/alpha_inv/.style={column name={$\alpha(\mu)^{-1}$}, fixed, fixed zerofill, precision=9}}
\pgfplotstabletypeset[
  col sep=comma,
  header=true,
  string type,
  columns={mu,alpha,alpha_inv},
  every head row/.style={before row=\toprule,after row=\midrule},
  every last row/.style={after row=\bottomrule}
]{data/alpha_two_loop_grid.csv}

\subsection*{Computed lepton masses (from CSV)}
\pgfplotstableset{
  columns/name/.style={column name={Lepton}},
  columns/value/.style={column name={$m$}, fixed, fixed zerofill, precision=8},
  columns/uncertainty/.style={column name={uncertainty}, sci, sci zerofill, precision=2},
  columns/units/.style={column name={units}}
}
\pgfplotstabletypeset[
  col sep=comma,
  header=true,
  columns={name,value,uncertainty,units},
  every head row/.style={before row=\toprule,after row=\midrule},
  every last row/.style={after row=\bottomrule}
]{data/leptons.csv}
